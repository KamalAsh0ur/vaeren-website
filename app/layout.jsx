import './globals.css';

// This is a minimal root layout. The actual layout logic
// (fonts, meta pixel, RTL, etc.) lives in app/[lang]/layout.jsx.
export default function RootLayout({ children }) {
  return children;
}
